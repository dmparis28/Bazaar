// This is a placeholder to show how we share code.

export interface User {
  id: string;
  email: string;
  // We will expand this later
}

export interface Product {
  id: string;
  name: string;
  priceInCents: number;
  vendorId: string;
}